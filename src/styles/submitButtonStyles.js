import { StyleSheet } from 'react-native';

const buttonHeight = 27.5

const styles = StyleSheet.create({
    container: {
        height: buttonHeight,
        borderRadius: buttonHeight / 2,
    }
});


export default styles;
